
// function random mix list of products

function sortingRandom(arr) {

  arr.sort(() => Math.random() - 0.5);

}

// export functions

export default sortingRandom; // export random mix list of products
